failure_files = ["tests/acceptance/test_phase2_storage_commit.py", "tests/contract/test_disk_search.py", "tests/storage/test_commit_recovery.py"]

[[blocks]]
id = "mechanism"
title_en = "Atomic commit and reopen mechanism"
title_zh = "原子 Commit 与重开机制"
summary_en = "The writer flushes, prepares all referenced children, writes the next manifest, atomically swaps the root, and advances committed generation only after success."
summary_zh = "Writer 先 Flush、准备全部引用 Child、写下一 Manifest、原子 Swap Root，并只在成功后推进 Committed Generation。"
files = ["src/minilucene/index/directory.py", "src/minilucene/reader.py", "src/minilucene/writer.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["src/minilucene/__init__.py"]
supporting = true
