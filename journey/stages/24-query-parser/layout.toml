failure_files = ["tests/unit/query_parser/test_parser.py"]

[[blocks]]
id = "mechanism"
title_en = "Recursive-descent query parser mechanism"
title_zh = "递归下降 Query Parser机制"
summary_en = "Parsing advances tokens through OR, AND, unary, and primary functions, applies field scope deliberately, and requires full input consumption."
summary_zh = "Parsing 通过 OR、AND、Unary 与 Primary Function 推进 Token，有意应用 Field Scope，并要求完整消费输入。"
files = ["src/minilucene/analysis/standard.py", "src/minilucene/query_parser/parser.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["src/minilucene/query_parser/__init__.py"]
supporting = true
