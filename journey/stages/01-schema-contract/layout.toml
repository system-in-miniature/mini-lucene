failure_files = ["tests/contract/test_schema.py", "tests/test_public_surface.py"]

[[blocks]]
id = "mechanism"
title_en = "Field and document contract mechanism"
title_zh = "字段与文档契约机制"
summary_en = "Validation resolves every field definition before analysis or storage and returns typed failures before state changes."
summary_zh = "Validation 在 Analysis 或 Storage 前解析每个 Field Definition，并在状态变化前返回类型化失败。"
files = ["src/minilucene/document.py", "src/minilucene/schema.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["pyproject.toml", "src/minilucene/__init__.py", "uv.lock"]
supporting = true
