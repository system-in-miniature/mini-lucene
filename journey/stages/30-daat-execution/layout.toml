failure_files = ["tests/contract/test_collect_then_fetch.py", "tests/unit/search/test_daat_scorer.py", "tests/unit/search/test_iterators.py", "tests/unit/search/test_topk.py"]

[[blocks]]
id = "mechanism"
title_en = "Document-at-a-time execution mechanism"
title_zh = "Document-at-a-time 执行机制"
summary_en = "The planner builds iterator/scorer trees from rewritten queries, collectors consume candidates without stored fields, and a second phase fetches only winners."
summary_zh = "Planner 从 Rewritten Query 构建 Iterator/Scorer Tree，Collector 在不取 Stored Field 的情况下消费 Candidate，第二阶段只 Fetch Winner。"
files = ["src/minilucene/search/collector.py", "src/minilucene/search/iterators.py", "src/minilucene/search/scorer.py", "src/minilucene/search/searcher.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["src/minilucene/search/__init__.py"]
supporting = true
