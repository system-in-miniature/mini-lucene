failure_files = ["tests/unit/storage/test_segment_image.py"]

[[blocks]]
id = "mechanism"
title_en = "Immutable segment images mechanism"
title_zh = "不可变 Segment Image机制"
summary_en = "Builders normalize maps and tuples, validate cross-table counts and doc IDs, and expose one deterministic image to codecs."
summary_zh = "Builder 归一化 Map 与 Tuple，校验跨表 Count 与 Doc ID，再向 Codec 暴露唯一确定 Image。"
files = ["src/minilucene/storage/image.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["src/minilucene/storage/__init__.py"]
supporting = true
